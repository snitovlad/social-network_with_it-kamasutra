let initialState = {
   friends: [
      { id: 1, name: "Andrey" },
      { id: 2, name: "Sasha" },
      { id: 3, name: "Sveta" }
   ]
}

const sidebarReducer = (state = initialState, action) => {

   return state;
}

export default sidebarReducer;